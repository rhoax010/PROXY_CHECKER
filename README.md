# PROXY_CHECKER
A multithreaded Python tool to validate free proxies from a .txt list. Supports http, socks4, and socks5 protocols. Displays results in the terminal with live/dead status, color-coded output, and public IP detection. Includes built-in dependency checks and links to trusted proxy sources.
